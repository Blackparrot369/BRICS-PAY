[package]
name = "brics-ledger"
version = "0.1.0"
edition = "2021"
description = "Blockchain ledger implementation for BRICS Pay"
license = "MIT"

[dependencies]
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
brics-crypto = { path = "../crypto" }

[dev-dependencies]
tempfile = "3.8"
